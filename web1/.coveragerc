[run]
source = .
omit =
    */migrations/*
    */tests.py
    */__init__.py
    */tests/*
    algorithms/run_example.py
    */js/*
    /mnt2/ubuntu/twoxar-demo/databases/*

# Coverage pulls in the ETL databases source due to the way we source version info from there,
# but we don't really want to include those, they are tested separately if at all.
